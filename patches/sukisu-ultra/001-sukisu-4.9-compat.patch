diff --git a/feature/selinux_hide.c b/feature/selinux_hide.c
index 31b2de8..f6a68d7 100644
--- a/feature/selinux_hide.c
+++ b/feature/selinux_hide.c
@@ -36,7 +36,11 @@ extern void context_struct_compute_av_fn(struct policydb *policydb,
 struct selinux_state fake_state;
 #endif
 #ifdef KSU_COMPAT_USE_STATIC_KEY
-DEFINE_STATIC_KEY_FALSE(fake_status_initialize_key);
+// SukiSU 4.9 compatibility: keep the static key storage compatible with old clang.
+__maybe_static DEFINE_STATIC_KEY_FALSE(fake_status_initialize_key);
+#else
+static bool fake_status_initialize_key __read_mostly = false;
 #endif
 struct page *fake_status = NULL;
 
@@ -78,9 +82,13 @@ void ksu_selinux_hide_handle_second_stage()
     initialize_fake_status();
     // https://github.com/torvalds/linux/blame/e8c2f9fdadee7cbc75134dc463c1e0d856d6e5c1/security/selinux/selinuxfs.c#L2014
     if (fake_status)
+#ifdef KSU_COMPAT_USE_STATIC_KEY
         static_key_disable(&fake_status_initialize_key.key);
+#else
+        fake_status_initialize_key = true;
+#endif
     else
         pr_warn("selinux_hide: fake status need late initialization\n");
 }
 
 void ksu_selinux_hide_handle_post_fs_data()
 {
+#ifdef KSU_COMPAT_USE_STATIC_KEY
     static_key_disable(&fake_status_initialize_key.key);
+#else
+    fake_status_initialize_key = true;
+#endif
     if (!fake_status)
         pr_err("selinux_hide: fake status is not initialized after post-fs-data!\n");
 }
diff --git a/infra/kernel_compat.h b/infra/kernel_compat.h
index 5b10ee7..bea39fb 100644
--- a/infra/kernel_compat.h
+++ b/infra/kernel_compat.h
@@ -262,4 +262,29 @@
 #endif
 
+#ifndef __has_attribute
+#define __has_attribute(x) __GCC4_has_attribute_##x
+#define __GCC4_has_attribute___fallthrough__ 0
+#endif
+
+#if __has_attribute(__fallthrough__)
+#define fallthrough __attribute__((__fallthrough__))
+#else
+#define fallthrough do { } while (0)
+#endif
+
 #endif
diff --git a/policy/allowlist.c b/policy/allowlist.c
index fa32ac1..19e15d0 100644
--- a/policy/allowlist.c
+++ b/policy/allowlist.c
@@ -1,3 +1,5 @@
+#include "infra/kernel_compat.h"
+
 #define FILE_MAGIC 0x7f4b5355 // ' KSU', u32
 #define FILE_FORMAT_VERSION 4 // u32
 
diff --git a/runtime/ksud.c b/runtime/ksud.c
index c9d11da..1f32d11 100644
--- a/runtime/ksud.c
+++ b/runtime/ksud.c
@@ -53,8 +53,7 @@ void on_post_fs_data(void)
     ksu_load_allow_list();
     ksu_observer_init();
     // sanity check, this may influence the performance
     stop_input_hook();
-    ksu_selinux_hide_handle_post_fs_data();
 }
 
 extern void ext4_unregister_sysfs(struct super_block *sb);
@@ -288,7 +288,6 @@ int ksu_handle_execveat_ksud(int *fd, struct filename **filename_ptr,
         if (!init_second_stage_executed && check_argv(*argv, 1, "second_stage", buf, sizeof(buf))) {
             pr_info("/system/bin/init second_stage executed\n");
-            ksu_selinux_hide_handle_second_stage();
             apply_kernelsu_rules();
             cache_sid();
             setup_ksu_cred();
diff --git a/sulog/event.c b/sulog/event.c
index e45f01f..9a1b25c 100644
--- a/sulog/event.c
+++ b/sulog/event.c
@@ -142,7 +142,7 @@ static __u32 ksu_sulog_flatten_argv(struct user_arg_ptr *argv_user, char *dst, _
         copied =
-            strncpy_from_user_nofault(arg, (const void __user *)untagged_addr((unsigned long)arg_user), sizeof(arg));
+            ksu_strncpy_from_user_nofault(arg, (const void __user *)untagged_addr((unsigned long)arg_user), sizeof(arg));
         if (copied <= 0)
             return ksu_sulog_copy_empty_string(dst);
 
@@ -374,7 +374,7 @@ static struct ksu_sulog_pending_event *ksu_sulog_capture_grant_root(const struct
     struct ksu_sulog_event *event;
 
     // This is actually stupid fix
-    #define USER_ARG_NULL user_arg_null_ptr()
+    #define USER_ARG_NULL (&(struct user_arg_ptr){ 0 })
 
     pending = ksu_sulog_capture(KSU_SULOG_EVENT_IOCTL_GRANT_ROOT, NULL, USER_ARG_NULL, gfp);
     if (!pending)
